import {addToCart} from '../actions/add-to-cart';
import {produce} from 'immer';

const data = produce((draft,action)=>{
    // switch(action.type){
    //     case "ADD_CART":
    //         console.log(action);
    //         return;
    // }
})
export default data;
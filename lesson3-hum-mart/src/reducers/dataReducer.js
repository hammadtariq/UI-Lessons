import {produce} from 'immer';
const data = produce((draft,action)=>{
  switch(action.type){
    // case 'SHOW_DATA':
    //     return draft.value;
    default:
    return draft
  }
   
})
export default data